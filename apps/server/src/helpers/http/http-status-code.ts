/* eslint-disable no-unused-vars */
export enum HttpStatusCode {
  BAD_REQUEST = 400,
  OK = 200,
  SERVER_ERROR = 500,
  UNAUTHORIZED_ERROR = 401,
  CONFLICT = 409,
  NOT_FOUND = 404,
  FORBIDDEN = 403,
}
